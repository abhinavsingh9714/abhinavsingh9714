# ── Chat mode ────────────────────────────────────────────────────────────────
# "mock"  → local generator, no network (default for dev + static Vercel deploys)
# "api"   → POST /api/chat SSE route (set this once you wire a real LLM backend)
NEXT_PUBLIC_CHAT_MODE=mock

# ── LLM backend (only needed when NEXT_PUBLIC_CHAT_MODE=api) ─────────────────
# These are SERVER-SIDE env vars (no NEXT_PUBLIC_ prefix) — never exposed to the
# browser. Set them in the Vercel dashboard under Settings → Environment Variables.
#
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=...
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# PINECONE_API_KEY=...
# PINECONE_INDEX=portfolio-rag

# ── Analytics / monitoring (optional) ────────────────────────────────────────
# NEXT_PUBLIC_POSTHOG_KEY=phc_...
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com
